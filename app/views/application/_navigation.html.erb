<nav>
  <div class="navbar navbar-fixed-top">
    <div class="navbar">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to "Nerdlock", root_path, :class => "brand logo active" %>
          <div class="nav-collapse">
            <%= render :partial => 'search_form' %>
            <ul class="nav">
              <li class='<%= "active" if params[:controller] == "questions" %>'><%= link_to "Questions", root_path %></li>
              <li class='<%= "active" if params[:controller] == "activities" %>'><%= link_to "Feed", activities_path %></li>
              <li class='<%= "active" if params[:controller] == "profiles" %>'><%= link_to "Profile", profile_path %></li>
              <li class='<%= "active" if params[:controller] == "notifications" %>'><%= link_to "Messages", notifications_path %></li>
              <%= content_tag :li, id: "notification-bubble" do %>
                  <%= link_to @new_notifications.size, notifications_path, {class: "badge btn-danger" } %>
              <% end     if @new_notifications.any? %>

            </ul>

            <ul class="nav">
              <li class="divider-vertical"></li>
              <li>
                <%= image_tag current_user.avatar.thumb.url, :alt => '' %>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.full_name %>
                  <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Log Out", destroy_user_session_path, :method => :delete %></li>
                  <li><%= link_to "Edit your profile", edit_profile_path %>
                  </li>
                </ul>
              </li>
            </ul>
          </div>
          <!-- /.nav-collapse -->
        </div>
      </div>
      <!-- /navbar-inner -->
    </div>
</nav>